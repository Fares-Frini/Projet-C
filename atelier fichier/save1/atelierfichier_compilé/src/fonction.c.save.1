#include "fonction.h"
#include<stdio.h>

int ajouter(inscri p,char filename[], char nom[], char [] prenom, char username[] , char password[], int role)
{
FILE *  f=fopen(filename,"a");
if (f!=NULL)
{
p.nom=nom;
p.prenom=prenom;
p.username=username;
p.password=password;
p.role=role;
fprintf(f,"%s %s %s %s %d",p.nom,p.prenom,p.username,p.password,p.role);
fclose(f);
return 1 ;
}
else return 0;
}
int login(char	 username[],char password[], char filename[])
{

}
